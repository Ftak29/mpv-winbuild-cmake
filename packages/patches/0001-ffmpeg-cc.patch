diff --git a/libavcodec/ccaption_dec.c b/libavcodec/ccaption_dec.c
index 9b6c766d87..0f2228d193 100644
--- a/libavcodec/ccaption_dec.c
+++ b/libavcodec/ccaption_dec.c
@@ -20,6 +20,7 @@
  */
 
 #include <assert.h>
+#include <string.h>
 #include "avcodec.h"
 #include "ass.h"
 #include "codec_internal.h"
@@ -33,6 +34,25 @@
 #define UNSET_FLAG(var, val) ( (var) &=  ~( 1 << (val)) )
 #define CHECK_FLAG(var, val) ( (var) &    ( 1 << (val)) )
 
+#ifdef __ANDROID__
+#include <android/log.h>
+#define CCLOG(...) __android_log_print(ANDROID_LOG_ERROR, "CC608DBG", __VA_ARGS__)
+#else
+#include <stdio.h>
+#include <stdarg.h>
+static void CCLOG(const char *fmt, ...)
+{
+    FILE *f = fopen("C:\\temp\\cc608dbg.txt", "a");
+    if (!f)
+        return;
+    va_list args;
+    va_start(args, fmt);
+    vfprintf(f, fmt, args);
+    va_end(args);
+    fclose(f);
+}
+#endif
+
 static const AVRational ms_tb = {1, 1000};
 
 enum cc_mode {
@@ -386,13 +406,11 @@ static int validate_cc_data_pair(const uint8_t *cc_data_pair, uint8_t *hi)
         return AVERROR_INVALIDDATA;
 
     // if EIA-608 data then verify parity.
-    if (cc_type==0 || cc_type==1) {
-        if (!av_parity(cc_data_pair[2])) {
+    if (cc_type == 0 || cc_type == 1) {
+        if (!av_parity(cc_data_pair[2]) || !av_parity(cc_data_pair[1])) {
+            // drop pair instead of poisoning decoder
             return AVERROR_INVALIDDATA;
         }
-        if (!av_parity(cc_data_pair[1])) {
-            *hi = 0x7F;
-        }
     }
 
     //Skip non-data
@@ -501,7 +519,8 @@ static int capture_screen(CCaptionSubContext *ctx)
 
             x = ASS_DEFAULT_PLAYRESX * (0.1 + 0.0250 * j);
             y = ASS_DEFAULT_PLAYRESY * (0.1 + 0.0533 * i);
-            av_bprintf(&ctx->buffer[bidx], "{\\an7}{\\pos(%d,%d)}", x, y);
+            //av_bprintf(&ctx->buffer[bidx], "{\\an7}{\\pos(%d,%d)}", x, y);
+            av_bprintf(&ctx->buffer[bidx], "{\\an2}");
 
             for (; j < SCREEN_COLUMNS; j++) {
                 const char *e_tag = "", *s_tag = "", *c_tag = "", *b_tag = "";
@@ -621,6 +640,57 @@ static void update_time(CCaptionSubContext *ctx, int64_t pts)
     ctx->buffer_time[1] = pts;
 }
 
+static int cc608_visible_text_score(const char *s)
+{
+    int score = 0;
+    int in_tag = 0;
+
+    if (!s)
+        return 0;
+
+    for (; *s; s++) {
+        if (in_tag) {
+            if (*s == '}')
+                in_tag = 0;
+            continue;
+        }
+
+        if (*s == '{') {
+            in_tag = 1;
+            continue;
+        }
+
+        if (*s == '\\') {
+            if (s[1] == 'N' || s[1] == 'h') {
+                s++;
+                continue;
+            }
+        }
+
+        if (*s == ' ' || *s == '\r' || *s == '\n' || *s == '\t')
+            continue;
+
+        score++;
+    }
+
+    return score;
+}
+
+static int cc608_should_emit_realtime(const char *s)
+{
+    int score = cc608_visible_text_score(s);
+
+    if (!s || !s[0])
+        return 0;
+
+    /* Suppress empty / tiny incremental paints like "EX", "TR", quotes, etc. */
+    if (score < 8)
+        return 0;
+
+    return 1;
+}
+
+
 static void handle_bgattr(CCaptionSubContext *ctx, uint8_t hi, uint8_t lo)
 {
     const int i = (lo & 0xf) >> 1;
@@ -867,17 +937,20 @@ static int decode(AVCodecContext *avctx, AVSubtitle *sub,
     unsigned nb_rect_allocated = 0;
 
     for (i = 0; i < len; i += 3) {
-        uint8_t hi, cc_type = bptr[i] & 1;
-
-        if (ctx->data_field < 0)
-            ctx->data_field = cc_type;
+        uint8_t hi;
+        uint8_t cc_type = bptr[i] & 3;
+        uint8_t field = cc_type & 1;
 
         if (validate_cc_data_pair(bptr + i, &hi))
             continue;
 
-        if (cc_type != ctx->data_field)
-            continue;
+        if (ctx->data_field < 0 && hi != 0 && bptr[i + 2] != 0) {
+            ctx->data_field = field;
+        }
 
+        if (field != ctx->data_field)
+            continue;
+          
         ret = process_cc608(ctx, hi & 0x7f, bptr[i + 2] & 0x7f);
         if (ret < 0)
             return ret;
@@ -893,6 +966,11 @@ static int decode(AVCodecContext *avctx, AVSubtitle *sub,
 
         if (ctx->buffer[bidx].str[0] || ctx->real_time) {
             ff_dlog(avctx, "cdp writing data (%s)\n", ctx->buffer[bidx].str);
+            CCLOG("EMIT_NORMAL pts=%lld text=%s\n",
+                  (long long)ctx->buffer_time[0],
+                  ctx->buffer[bidx].str ? ctx->buffer[bidx].str : "");
+            start_time = ctx->buffer_time[0];
+            
             start_time = ctx->buffer_time[0];
             sub->pts = start_time;
             end_time = ctx->buffer_time[1];
@@ -911,6 +989,9 @@ static int decode(AVCodecContext *avctx, AVSubtitle *sub,
 
     if (!bptr && !ctx->real_time && ctx->buffer[!ctx->buffer_index].str[0]) {
         bidx = !ctx->buffer_index;
+        CCLOG("EMIT_FLUSH pts=%lld text=%s\n",
+              (long long)ctx->buffer_time[1],
+              ctx->buffer[bidx].str ? ctx->buffer[bidx].str : "");        
         ret = ff_ass_add_rect2(sub, ctx->buffer[bidx].str, ctx->readorder++, 0, NULL, NULL, &nb_rect_allocated);
         if (ret < 0)
             return ret;
@@ -930,12 +1011,11 @@ static int decode(AVCodecContext *avctx, AVSubtitle *sub,
         capture_screen(ctx);
         ctx->buffer_changed = 0;
 
-        ret = ff_ass_add_rect2(sub, ctx->buffer[bidx].str, ctx->readorder++, 0, NULL, NULL, &nb_rect_allocated);
-        if (ret < 0)
-            return ret;
-        sub->end_display_time = -1;
+        CCLOG("SKIP_REALTIME_DISABLED pts=%lld text=%s\n",
+              (long long)sub->pts,
+              ctx->buffer[bidx].str ? ctx->buffer[bidx].str : "");
     }
-
+    
     *got_sub = sub->num_rects > 0;
     return avpkt->size;
 }
