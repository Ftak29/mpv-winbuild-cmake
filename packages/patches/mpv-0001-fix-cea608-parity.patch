--- a/sub/dec_sub.c
+++ b/sub/dec_sub.c
@@ -332,6 +332,24 @@ struct sub_bitmaps *sub_get_bitmaps(struct dec_sub *sub, struct mp_osd_res dim,
     return res;
 }
 
+static void strip_cea608_parity(char *text)
+{
+    if (!text)
+        return;
+
+    for (char *p = text; *p; p++) {
+        unsigned char c = (unsigned char)*p;
+
+        // remove CEA-608 parity bit
+        c &= 0x7F;
+
+        if ((c < 32 || c == 127) && c != '\n')
+            c = ' ';
+
+        *p = (char)c;
+    }
+}
+
 // The returned string is talloc'ed.
 char *sub_get_text(struct dec_sub *sub, double pts, enum sd_text_type type)
 {
@@ -344,8 +362,10 @@ char *sub_get_text(struct dec_sub *sub, double pts, enum sd_text_type type)
     sub->last_vo_pts = pts;
     update_segment(sub);
 
-    if (sub->sd->driver->get_text)
+    if (sub->sd->driver->get_text) {
         text = sub->sd->driver->get_text(sub->sd, pts, type);
+        strip_cea608_parity(text);
+    }
     mp_mutex_unlock(&sub->lock);
     return text;
 }
 
